build/
bench_results.json
*.jsonl

# WASM build artifacts
wasm/*.js
wasm/*.wasm
wasm/*.wasm.map

# Web (Next.js)
web/node_modules/
web/.next/
web/out/
web/.env*.local

# web/public/wasm/ is intentionally NOT ignored: Vercel's build environment
# has no Emscripten, so the compiled WASM module is committed as a build
# artifact. Rebuild via wasm/build.sh after any engine change and commit
# the output.

# macOS
.DS_Store
